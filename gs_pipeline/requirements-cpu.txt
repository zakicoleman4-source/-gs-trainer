# Dependencies needed for CPU-only unit tests, the Streamlit UI, and the
# Metashape-side export script (when run outside Metashape for dry-run tests).
# The full trainer needs the GPU stack in requirements-gpu.txt as well.
numpy>=1.26
plyfile>=1.0
pyyaml>=6.0
pillow>=10.0
watchdog>=4.0
streamlit>=1.36
pytest>=8.0
